import { medicine } from '@app/mockups/medicine';
import { meetings } from '@app/mockups/meetings';
import { counterparties } from '@app/mockups/counterparties';
import {files} from '@app/mockups/files';

export const MOCK = {
    medicine,
    meetings,
    counterparties,
    files,
};
